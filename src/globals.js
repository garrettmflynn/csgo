export const GAME_MONITOR_PORT = 3000;
export const EVENT_SERVER_PORT = 3001;

export const HOST = '127.0.0.1';

export const GAME_MONITOR_URI = `http://${HOST}:${GAME_MONITOR_PORT}`;
export const EVENT_SERVER_URI = `http://${HOST}:${EVENT_SERVER_PORT}`;

export const filename = 'gamestate_integration_hypergamma.cfg'
